# 第三棒验证手册

> 目的：让后续同学（第四棒）能在不需要我参与的情况下，**逐条验证**第三棒的交付物并发现未完成的未来依赖问题。
>
> 基地：`/Users/zhuanzmima0000/user/ARY-Work/arypart2 2/ary-grs-003-nihao-haha/`

## 0. 必读：先看 0.1 风险警告

| 编号 | 警告 | 重要性 |
|------|------|--------|
| W1 | 第一棒 schema 与 NextAuth PrismaAdapter 不兼容 | **致命** — 不修 A2 验收会卡死 |
| W2 | 跨平台（macOS vs Windows）问题 | 中等 — Prisma + esbuild 都要重装 |
| W3 | NextAuth database session + Edge Runtime 限制 | 中等 — middleware 不能读 role |
| W4 | `signIn()` from `next-auth/react` 需要 SessionProvider | 低 — 用 form action 绕过 |

W1 是 **A2 验收的必堵坑**。如果不修，下面所有 A2 之后的验收都走不通。

详见 `/Users/zhuanzmima0000/user/ARY-Work/arypart2 2/ary-grs-003-nihao-haha/第三棒执行计划.md` 的"已知问题"段。

---

## 1. 启动验证

```bash
cd "/Users/zhuanzmima0000/user/ARY-Work/arypart2 2/ary-grs-003-nihao-haha"
npm install
npx prisma generate
npx prisma db push
npm run db:seed
```

| 步骤 | 期望 | 失败时 |
|------|------|--------|
| 1.1 `npm install` | 0 errors | macOS 报 esbuild 平台错 → 见 W2 |
| 1.2 `npx prisma generate` | `✔ Generated Prisma Client` | 报 darwin-arm64 错 → 见 W2 |
| 1.3 `npx prisma db push` | `Database is now in sync` | 检查 DATABASE_URL 路径 |
| 1.4 `npm run db:seed` | `Seed completed successfully!` | 看 stack trace 定位 |

```bash
npm run dev
```

| 步骤 | 期望 | 失败时 |
|------|------|--------|
| 1.5 dev server 启动 | `Ready in 2-3s` | 看 `/tmp/ary-dev.log` 或 stdout |

**关键**：dev server 启动后**不要立即测试**，等 3 秒让 Next.js 编译完成。

---

## 2. 端到端健康检查

```bash
# 1. 公开端能渲染
curl -s -o /dev/null -w "HTTP %{http_code}\n" http://localhost:3000/
# 期望 200

# 2. NextAuth providers 正常
curl -s http://localhost:3000/api/auth/providers
# 期望: {"github":{"id":"github","name":"GitHub","type":"oauth",...}}

# 3. CSRF token 可获取
curl -s http://localhost:3000/api/auth/csrf
# 期望: {"csrfToken":"..."}

# 4. Admin API 无 session 应被拦截
curl -s -o /dev/null -w "HTTP %{http_code}\n" http://localhost:3000/api/admin/users
# 期望 307 (重定向到 /login)
```

---

## 3. 浏览器手动验收（28 条）

### A. 登录与资料补全（6 条）

#### A1. 未登录访问 /console → /login

- 操作：浏览器输入 `http://localhost:3000/console`
- 期望：浏览器跳转到 `http://localhost:3000/login`
- 失败：停在 `/console` 空白 → 中间件没配

#### A2. GitHub OAuth 登录完整链路

**这是最复杂、最容易出问题的环节**。需要分 4 步走：

**A2.1 在登录页点 Sign in with GitHub**
- 期望：浏览器跳转到 `https://github.com/login/oauth/authorize?client_id=...&scope=...`
- **失败标志**：
  - 仍在 `/login` 没动 → 按钮没绑事件
  - 跳到 `github.com/...` 但 404 → 客户端 ID 是占位符
  - 跳到 `?error=Configuration` → 见 W1（schema 兼容）
  - 浏览器陷入等待 → form action 兼容问题

**A2.2 在 GitHub 授权页点 Authorize**
- 期望：跳回 `http://localhost:3000/api/auth/callback/github?code=...`
- 失败：GitHub 报 redirect_uri 错 → 检查 GitHub OAuth App 的 `Authorization callback URL` 必须是 `http://localhost:3000/api/auth/callback/github`

**A2.3 NextAuth 拿 access_token + 拉 user profile**
- 期望：浏览器跳转到 `http://localhost:3000/console` 或 `/console/profile/complete`
- **失败标志**：
  - `?error=Configuration` → W1 未修，PrismaAdapter 调 Prisma 报错
  - `?error=AccessDenied` → GitHub App 设置错误
  - 500 错误 → 看 dev log

**A2.4 在 /console/profile/complete 提交表单**
- 表单字段：name、school、bio（**只有 3 个**，不要 company）
- 期望：跳转 `/console`，显示 "Welcome, {name}"

**A2 关键诊断**：
```bash
# 看 dev log
tail -50 /tmp/ary-dev.log

# 关键错误关键词
[auth][error] / [auth][cause] / UnknownAction / PrismaClientValidationError / MissingCSRF
```

#### A3. 资料补全页字段数

- 操作：URL 直接访问 `/console/profile/complete`（需未登录 session 中 `profileCompleted = true`）
- 期望：看到 3 个字段（name 必填、school、bio），**没有** company
- 失败：4 个字段 → 多加了 company（AI 自行发明需求）

#### A4. 已补全用户访问 /complete

- 操作：完成 A2 后再访问 `/console/profile/complete`
- 期望：自动跳回 `/console`（不能再次进入补全页）
- 失败：仍能进入 → 守卫逻辑错误

#### A5. 未补全用户访问 /console

- 操作：注册新 GitHub 账号登录（首次登录 `profileCompleted = false`）
- 期望：访问 `/console` 自动跳 `/console/profile/complete`
- 失败：直接进 Console Home → 守卫漏了

#### A6. Header 头像

- 操作：登录成功后看 Header 右上角
- 期望：Workspace 按钮前显示**圆形头像**（GitHub 头像），加载失败时显示首字母占位符
- 失败：只有 Workspace 按钮没头像 → Phase A1 没做

---

### B. Admin Console（8 条）

#### B1. Admin 访问 /console/admin
- 用户：`emma` 或 `admin`（admin role）
- 期望：用户表格，至少 6 列：User / GitHub ID / Roles / Profile / Last Sign-in / (操作)
- 失败：缺 "Last Sign-in" 列 → Phase B1 没做

#### B2. 非 admin 访问 /console/admin
- 用户：`bob`（rider role）
- 期望：浏览器 URL 变 `/console/access-denied`（**URL 必须变**）
- 失败：URL 仍在 `/console/admin` 但内容是 "Access Denied" → 真正的守卫没做

#### B3. 角色编辑弹窗
- 操作：在 B1 页面点 "Edit Roles"
- 期望：居中模态 + 4 个复选框（rider / judge / organizer / admin）+ Cancel / Save
- 失败：弹窗打不开 / 缺角色 / 没预选当前用户角色

#### B4. 编辑并保存
- 操作：改一个用户角色，点 Save
- 期望：弹窗关闭，表格更新；Network 看到 `PUT /api/admin/users/[id]/roles` 返回 200
- 失败：路径错（应是 `/roles` 子路径，不是 `/[id]`）

#### B5. API 路径正确性
```bash
curl -X PUT http://localhost:3000/api/admin/users/<id>/roles \
  -H "Content-Type: application/json" \
  -d '{"roles":["rider"]}'
```
- 期望：HTTP 200 + `{"success":true,"roles":["rider"]}`
- 失败：404 → 路径错了

#### B6. 取消保存
- 操作：开弹窗，点 Cancel
- 期望：弹窗关闭，表格不变

#### B7. 点击遮罩关闭
- 操作：开弹窗，点击弹窗外部黑色区域
- 期望：弹窗关闭

#### B8. Last Sign-in 列显示
- 期望：每行有值（时间或 "Never"），不空不 undefined

---

### C. Console Shell 布局（6 条）

#### C1. 顶部栏存在
- 操作：访问 `/console` 任意子页
- 期望：浏览器顶部固定栏（64px 高），左侧 "ARY Console" logo，右侧用户头像 + 名称 + Sign Out
- 失败：没有顶部栏

#### C2. Race 页面顶部栏显示 race 标题
- 操作：访问 `/console/races/genesis-dogfood-race/organizer`
- 期望：顶部栏除 logo 外还显示当前 race 标题
- 失败：只有 logo 没 race 标题

#### C3. 左侧栏（侧边栏）
- 期望：256px 宽，左侧固定；含 Home、Admin Console（admin）、My Races、My Rides、Judge View、Screen Console；底部用户信息卡
- 失败：没侧边栏 / 菜单项与角色不匹配

#### C4. 主内容区布局
- 期望：主区紧贴侧边栏右侧，不溢出，无横向滚动条

#### C5. 退出按钮位置
- 期望：在顶部栏右侧或侧边栏底部
- 失败：找不到 / 点击没反应

#### C6. 公开端 Header 不受影响
- 操作：访问 `/`（首页）
- 期望：公开端 Header 显示，不是 ConsoleShell 侧边栏
- 失败：公开端也出现侧边栏

---

### D. 路由守卫（8 条）

#### D1. 未登录访问 /console/admin/users
- 操作：清 cookies，访问 `/console/admin/users`
- 期望：跳 `/login`
- 失败：停在 admin URL 空白

#### D2. 未登录访问 race 页面
- 操作：清 cookies，访问 `/console/races/xxx/organizer`
- 期望：跳 `/login`

#### D3. 非 organizer 访问 race organizer
- 操作：以 `bob`（rider）登录，访问 `/console/races/genesis-dogfood-race/organizer`
- 期望：跳 `/console/access-denied`

#### D4. 非 rider 访问 race rider
- 操作：以 `emma`（organizer）登录，访问 `/console/races/xxx/rider`
- 期望：跳 `/console/access-denied`

#### D5. 非 judge 访问 race judge
- 操作：以 `bob`（rider）登录，访问 `/console/races/xxx/judge`
- 期望：跳 `/console/access-denied`

#### D6. 该 race 的 organizer 访问自己 organizer
- 操作：以 `genesis-dogfood-race` 的 organizer 身份登录，访问 `/console/races/genesis-dogfood-race/organizer`
- 期望：正常进入
- 失败：错误拦截 → 资源级守卫误判

#### D7. 跨赛事 organizer 拦截
- 操作：以 `genesis-dogfood-race` 的 organizer 身份登录，访问 `/console/races/bay-area-happy-trip/organizer`（**不是**该 organizer 管的）
- 期望：跳 `/console/access-denied`
- 失败：仍能进入 → managed race 检查没做

#### D8. 20 个占位页全部 200
**Organizer（11 个）**：
```
/console/races/genesis-dogfood-race/organizer
/console/races/genesis-dogfood-race/organizer/settings
/console/races/genesis-dogfood-race/organizer/registrations
/console/races/genesis-dogfood-race/organizer/riders
/console/races/genesis-dogfood-race/organizer/ca-status
/console/races/genesis-dogfood-race/organizer/works
/console/races/genesis-dogfood-race/organizer/judges
/console/races/genesis-dogfood-race/organizer/judging
/console/races/genesis-dogfood-race/organizer/awards
/console/races/genesis-dogfood-race/organizer/reports
/console/races/genesis-dogfood-race/organizer/maintenance
```

**Rider（6 个）**：
```
/console/races/genesis-dogfood-race/rider
/console/races/genesis-dogfood-race/rider/ca-setup
/console/races/genesis-dogfood-race/rider/riding
/console/races/genesis-dogfood-race/rider/submission
/console/races/genesis-dogfood-race/rider/review
/console/races/genesis-dogfood-race/rider/report
```

**Judge（3 个）**：
```
/console/races/genesis-dogfood-race/judge/assigned
/console/races/genesis-dogfood-race/judge/reviewing
/console/races/genesis-dogfood-race/judge/submitted
```

- 期望：每个返回 200，页面有标题 + 子导航 + "This section will be implemented in relay 4." 提示

---

## 4. Build 验收

```bash
cd "/Users/zhuanzmima0000/user/ARY-Work/arypart2 2/ary-grs-003-nihao-haha"
npm run build 2>&1 | tail -50
```

| 检查 | 期望 | 失败时 |
|------|------|--------|
| 编译 | `Compiled successfully` | 看错误 |
| 类型 | `Linting and checking validity of types ...` 通过 | 报 `Type error: 'RequireRole' cannot be used as a JSX component` → 改用**同步**组件 |
| 路由 | 50+ 路由编译成功 | 看 build 日志 |
| 警告 | 可能 darwin-arm64 warning（不阻塞） | 见 W2 |

---

## 5. 失败诊断决策树

### 症状 1：`/login?error=Configuration` 出现

**含义**：OAuth 回调阶段 Prisma 操作失败。

**根因**：
- W1：PrismaAdapter 调 `prisma.user.create` 时 schema 不匹配
- W1 证据：dev log 含 `PrismaClientValidationError`

**修复**（请参考第三棒执行计划 §问题 1）：

**方案 B（推荐）**：
```ts
// 新建 src/lib/auth-adapter.ts
import { PrismaAdapter } from "@auth/prisma-adapter";
import { prisma } from "./db";

export function authAdapter() {
  const base = PrismaAdapter(prisma);
  return {
    ...base,
    createUser: async (u: any) => base.createUser({ ...u, avatar: u.image }),
    updateUser: async (u: any) => base.updateUser({ ...u, ...(u.image !== undefined ? { avatar: u.image } : {}) }),
  };
}
```

**改 auth.config.ts**：
```ts
import { authAdapter } from "./auth-adapter";
// ...
adapter: authAdapter(),
```

### 症状 2：浏览器点 Sign in 按钮"陷入等待"

**含义**：`signIn()` from `next-auth/react` 在没有 SessionProvider 时卡住。

**修复**：用 form action 模式而不是客户端 signIn。

```tsx
<form action={async () => {
  "use server";
  await signIn("github", { redirectTo: "/console" });
}}>
  <button type="submit">Sign in with GitHub</button>
</form>
```

### 症状 3：dev log 报 `ConnectTimeoutError` 或 `UND_ERR_CONNECT_TIMEOUT`

**含义**：无法 reach `github.com`。

**修复**：在 dev server 启动时加代理：
```bash
HTTPS_PROXY=http://127.0.0.1:7897 HTTP_PROXY=http://127.0.0.1:7897 npm run dev
```

### 症状 4：build 报 `Type error: 'RequireRole' cannot be used as a JSX component`

**含义**：async function + 多个 return branch，React 18 TSX 类型不接受 `Promise<JSX>`。

**修复**：改用**同步**组件，调用方传入 `userRoles` prop：
```tsx
// 同步组件
export function RequireRole({ roles, userRoles, children, fallback }: Props) {
  const hasRole = roles.some(r => userRoles.includes(r));
  if (!hasRole) return <>{fallback ?? <AccessDenied />}</>;
  return <>{children}</>;
}
```

---

## 6. 给后续棒（第四棒）的备注

### 验收后要做

1. **commit 验收后的版本**（不含 `node_modules/` / `.next/` / `data/ary.db`）：
```bash
git add src/ prisma/ package.json package-lock.json
git commit -m "[macOS][feat][All]: 第三棒 登录 + Console 框架 完整闭环"
```

2. **更新** `第三棒SUMMARY.md` 和 `第三棒ai-task-tracker.md`（参照第二棒风格），包含本棒的全部执行记录、纠偏和验收证据。

3. **PLAN.md / STATUS.md** 加 `DEV-3` 状态。

4. **如果修了 W1**（schema 兼容），必须在文档里记录——这是越界修复。

### 已知待办

- U1: Live 实时 push / fetch（第五棒）
- U2: 断流 fallback（第五棒）
- U3: Screen Display 完整实现（第五棒）
- U4: User 公开 slug（Schema 迁移）
- U5: Report visibility（Schema 迁移）
- U6: JudgingRecord 发布标记（第四棒/Schema 迁移）
- U7: 自动化测试（第四/五棒）

---

## 7. 何时停止

每条验收条目都有"失败时"列。如果按诊断决策树修复后**仍失败**，**不要继续修改**——把症状、dev log、控制台截图写到 `第四棒交接.md` 转交第四棒或老师。

第三棒已陷入"修了 1 个问题暴露 2 个问题"的循环。继续修改只会让代码偏离计划更远。**停止是合理决策**。
