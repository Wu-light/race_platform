# 第三棒重新执行计划

> 基地：`/Users/zhuanzmima0000/user/ARY-Work/arypart2 2/ary-grs-003-nihao-haha/`
> 依据：`team-relay-plan.md` §4.4
> 原则：只做第三棒 scope 内的改动；不修第一棒 schema；不修环境问题；不写超出"plan 原文要求"的代码

## 第三棒 scope（plan §4.4）

| T | 内容 | 文件 |
|---|------|------|
| T3.1 | 登录完善 + Session 完整化 + Header 头像 + 资料补全页 + 登出 | `auth.config.ts` 改 session callback；新建 `profile/complete/page.tsx`；`Header.tsx` 加头像 |
| T3.2 | Admin Console 页面 + API | `console/admin/page.tsx` + `AdminUserTable.tsx` + `RoleEditor.tsx` + `api/admin/users/route.ts` + `api/admin/users/[id]/roles/route.ts` |
| T3.3 | Console Shell 框架 | `ConsoleShell.tsx` + `races/[slug]/layout.tsx` |
| T3.4 | 角色路由守卫 | `middleware.ts`（保留第1棒）+ `RequireRole.tsx` + `permissions.ts` 加资源级检查 + `races/[slug]/layout.tsx` |
| T3.5 | 20 个占位页 | Organizer(11) + Rider(6) + Judge(3) |
| T3.6 | Admin Console 完善 | 与 T3.2 同 |

## 已知问题（不在本棒 scope，但**必须告知第四棒**）

### 问题 1：第一棒 schema 与 NextAuth PrismaAdapter 不兼容

**事实**：第二棒跑的 NextAuth + 第一棒 schema 在 macOS 平台下登录会失败。涉及：

- `User.avatar` vs NextAuth 期望的 `User.image`
- `User.email` 缺 `@unique` 约束
- `User.githubId` 缺 nullability 兜底（`Int?`）

**症状**：`POST /api/auth/callback/github` 返回 302 redirect 到 `/login?error=Configuration`

**当前 dev log 实证**：
```
GET /api/auth/callback/github?code=58c98fc5d1a53e01f040 302 in 3127ms
GET /login?error=Configuration 200 in 22ms
[auth][error] UnknownAction / PrismaClientValidationError: Unknown argument `image`
```

**处理建议**（给第四棒，不归本棒）：

| 方案 | 描述 | 风险 |
|------|------|------|
| A. 改 schema 字段名 | `avatar → image`、`email @unique`、`githubId Int?` | 数据库需重建 + 种子重写 + 引用代码改 |
| B. 包装 PrismaAdapter | 重写 `createUser` / `updateUser` 把 `image → avatar` 映射 | 不改 schema，零迁移 |
| C. 跳过 GitHub 登录 | 本地 dev 用 mock user session | 不能完整验收 A2 流程 |

**推荐方案 B**（最小侵入，零数据库变动）：

```ts
// src/lib/auth-adapter.ts
import { PrismaAdapter } from "@auth/prisma-adapter";
import { prisma } from "./db";

export function authAdapter() {
  const base = PrismaAdapter(prisma);
  return {
    ...base,
    createUser: async (u: any) => base.createUser({ ...u, avatar: u.image }),
    updateUser: async (u: any) => base.updateUser({ ...u, ...(u.image !== undefined ? { avatar: u.image } : {}) }),
  };
}
```

### 问题 2：database session strategy + Edge Runtime 限制

**事实**：NextAuth v5 + database session + Next.js middleware 不能在 Edge Runtime 读 Prisma 拿 role。

**症状**：中间件层无法做 role-based URL 拦截。

**第三棒处理**：中间件只做 auth 检查；role 守卫放在 page/layout 层（`RequireRole` 组件、`races/[slug]/layout.tsx` 资源级守卫）。

### 问题 3：macOS 跨平台问题

**事实**：第一棒在 Windows 上 generate Prisma client、build esbuild。macOS 上 dev/build 都会失败。

**症状**：
- `PrismaClientInitializationError: Query Engine for darwin-arm64`
- `esbuild: needs @esbuild/darwin-arm64 instead of @esbuild/win32-x64`

**处理**（给后续棒）：
- `prisma/schema.prisma` 加 `binaryTargets = ["native", "darwin-arm64", "windows"]`
- `npm install @esbuild/darwin-arm64 --no-save`
- 重生成 Prisma client

## 第三棒执行步骤（按 plan §4.4 T3.1-T3.6 顺序）

### Step 1: 准备
```bash
cd "/Users/zhuanzmima0000/user/ARY-Work/arypart2 2/ary-grs-003-nihao-haha"
npm install
cp .env.example .env.local
# 填 GitHub OAuth（本地 dev 不必，但走通 A1-A2 需要）
npx prisma generate
npx prisma db push
npm run db:seed
```

### Step 2: 实施 T3.1（登录完善）

**改动**：
- `src/lib/auth.config.ts` — session callback 补 name/email/image/roles/profileCompleted
- `src/app/(console)/console/profile/complete/page.tsx` — 资料补全表单（name + school + bio，**不**含 company）
- `src/components/layout/Header.tsx` — 已登录态加 `Image` 头像

**不动**：
- 不改 schema.prisma
- 不动 `prisma/seed.ts`
- 不改 `auth.ts`

**已知坑**：用 `signIn("github", { redirectTo: "/console" })` server action 模式时，可能与 NextAuth v5 + database session 有 `Failed to fetch` 兼容性问题。**遇到时**改用 form action + `<form action={async () => { "use server"; await signIn(...); }}>`，让浏览器原生 POST 触发。

### Step 3: 实施 T3.2（Admin Console）

**新建**：
- `src/app/(console)/console/admin/page.tsx`
- `src/app/(console)/console/admin/AdminUserTable.tsx`
- `src/app/(console)/console/admin/RoleEditor.tsx`
- `src/app/api/admin/users/route.ts`（GET 列表）
- `src/app/api/admin/users/[id]/roles/route.ts`（PUT 更新角色）

**验证**：
- `GET /api/admin/users` 分页
- `PUT /api/admin/users/[id]/roles` 鉴权 admin + 过滤非法 role

### Step 4: 实施 T3.3（Console Shell）

**新建**：
- `src/components/layout/ConsoleShell.tsx`（侧边栏 + 顶栏 + currentRace prop）
- `src/app/(console)/console/layout.tsx`（套用 ConsoleShell）
- `src/app/(console)/console/races/[slug]/layout.tsx`（传 currentRace + 资源级守卫）

**修改**：
- `src/app/(console)/console/page.tsx` — 移除 `<Header />`，加 `profileCompleted` redirect 守卫

### Step 5: 实施 T3.4（路由守卫）

**新建/修改**：
- `src/components/layout/RequireRole.tsx`（**同步**组件，避免 React 18 async 类型问题）
- `src/lib/permissions.ts` — 加 `requireRiderForRace` / `requireJudgeForRace`
- `src/middleware.ts` — 保留第1棒（auth 保护 + admin API 403）

**资源级守卫**：在 `races/[slug]/layout.tsx` 中按 race 检查 user 是 organizer / rider / judge / admin 之一。

### Step 6: 实施 T3.5（20 占位页）

**新建**（用 subagent 批量生成 20 个 page.tsx + 3 个 Nav 组件）：
- `src/components/layout/OrganizerNav.tsx`
- `src/components/layout/RiderNav.tsx`
- `src/components/layout/JudgeNav.tsx`
- 11 个 `console/races/[slug]/organizer/{overview,settings,...}/page.tsx`
- 6 个 `console/races/[slug]/rider/{registration,ca-setup,...}/page.tsx`
- 3 个 `console/races/[slug]/judge/{assigned,reviewing,submitted}/page.tsx`

### Step 7: 实施 T3.6（Admin 完善）

T3.2 已含角色编辑弹窗。T3.6 是 T3.2 的 UI 层验收。

### Step 8: Build 验收

```bash
npm run build
```

期望：`Compiled successfully` + 50+ 路由编译通过。

## 越界禁区（**本棒严禁**）

| 操作 | 为什么禁止 |
|------|----------|
| 改 `prisma/schema.prisma` | 属于第一棒遗留问题，越界 |
| 改 `prisma/seed.ts` | 跟随 schema，越界 |
| 改 `.env.local` / `.env` | 用户操作，不归 Agent |
| 改跨平台（esbuild、binaryTargets） | 环境问题，越界 |
| 加 `Account` / `Session` / `VerificationToken` 字段 | schema 范围，越界 |
| 装新依赖 | 评估后决定，不在本棒 |

## 完成产物

- 22 个 src/ 新文件
- 1 个 src/lib/auth.config.ts 修改
- 1 个 src/components/layout/Header.tsx 修改
- 1 个 src/app/(console)/console/page.tsx 修改
- 1 个 src/middleware.ts 保持原状
- 1 个 src/lib/permissions.ts 新增 2 个函数
- 1 个 package.json 不动

## Build 验证

```bash
npm run build 2>&1 | tail -30
```

期望：
- `Compiled successfully`
- 50+ 路由编译成功
- `Type error: 'RequireRole' cannot be used as a JSX component` 需用**同步**组件（已记录）
